
dist_name="centos"
dist_vers="7.1.1503"
dist_arch="x86_64"

# build local for more recent virtualbox version
# atlas uses older virtualbox, vbga doesn't build/install
# on centos7
build_type="local"

# set in .bashrc
# ATLAS_USER_NAME="maier"
#
# default in atlas.sh is fine
# ATLAS_BOX_NAME="${dist_name}-${dist_vers}-${dist_arch}"

# END
